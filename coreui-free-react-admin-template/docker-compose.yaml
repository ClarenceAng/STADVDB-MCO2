version: "3.9"

services:
  webapp:
    build: .
    image: webapp:latest
    container_name: webapp
    ports:
      - "3000:3000"
      - "4000:4000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
